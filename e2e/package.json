{
  "name": "memi-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end (Playwright) proof that admin → DB → storefront is one connected system.",
  "scripts": {
    "test:e2e": "playwright test",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
